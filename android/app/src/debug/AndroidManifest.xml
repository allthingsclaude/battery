<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only components for the account-switching work. Nothing here ships.

  There is deliberately no probe *activity*: SpikeActivity is this project's one
  diagnostics harness and a second screen beside it was duplication.

  What remains is the receiver a probe card action fires at, kept only until
  PLAN_02 Phase 5 replaces it with the real one. The tile has already been
  promoted to main.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>

        <receiver
            android:name=".probe.ProbeReceiver"
            android:exported="false" />

    </application>
</manifest>
